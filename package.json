{
  "name": "agent-pixel-billboard",
  "version": "1.0.0",
  "description": "Agent-only pixel billboard on Base L2",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network baseMainnet"
  },
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^16.6.1",
    "hardhat": "^2.28.6"
  }
}
